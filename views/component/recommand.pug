div(class='recommand')
    table(class="ui celled table" id='newShareList')
        thead
            tr
                th(class='ten wide') 相关推荐

        tbody
            each result in results[1]
                tr
                    td
                        - var title = result.title;
                        a(class='text' href=`/result/?view=${result.ID}&kw=${results[1].kw}` target='_blank')
                            !=  title

